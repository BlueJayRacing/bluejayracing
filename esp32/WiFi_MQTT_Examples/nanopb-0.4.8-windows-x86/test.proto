syntax = "proto3";
import "nanopb.proto";


message Measurement {
    optional Timestamp timestamp = 1;
    repeated fixed32 data = 2 [(nanopb).max_count = 25];
}

message Timestamp {
    float ts = 1;
}