syntax = "proto3";

import "nanopb.proto";

message drive_data_t {
    repeated uint64 time = 2 [(nanopb).max_count = 1000, (nanopb).fixed_count = true];
    repeated float voltage = 3 [(nanopb).max_count = 1000, (nanopb).fixed_count = true];
};